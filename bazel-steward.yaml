update-rules:
  - name: "Bzlmod Dependencies"
    kinds:
      - name: "bzlmod"
        types:
          - "bazel_dep"
    paths:
      - "MODULE.bazel"

  - name: "Maven Dependencies"
    kinds:
      - name: "maven"
        types:
          - "maven.install"
    paths:
      - "MODULE.bazel"

pull-requests:
  enabled: true
  branch-prefix: "steward/"
  target: "main"
  limit: 10
  labels:
    - "dependencies"
  title-template: "⬆️ Update ${dependency} from ${currentVersion} to ${newVersion}"
  body-template: |
    ## Dependency Update
    
    📦 Dependency: `${dependency}`
    📈 Current version: `${currentVersion}`
    🎯 New version: `${newVersion}`
    
    ### Changelog
    ${changelog}
    
    ### Type of update
    ${updateType}

post-update-hooks:
  - pattern: ".*"
    commands:
      - "bazel build //..."
